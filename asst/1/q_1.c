#include <stdlib.h>
#include <stdio.h>
#include <math.h>

#define SIZE 5
#define RES_ARR SIZE *(SIZE - 1) / 2

int main()
{
    int arr[SIZE] = {1, 2, 3, 4, 6};
    int diff_arr[RES_ARR], square_arr[RES_ARR];
    int diff = 0;

    for (int k = 0; k < RES_ARR; k++)
    {
        for (int i = 0; i < SIZE - 1; i++)
        {
            for (int j = i + 1; j < SIZE; j++)
            {
                diff_arr[]
            }
        }
        diff_arr[k] = diff
    }
}